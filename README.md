# AnimePhoto Studio — MVP

Pequeña guía y puntos clave para construir, empaquetar y comercializar la app.

- Java 17 LTS (obligatorio)
- JavaFX (bundled via dependencies)
- Maven + badass-jlink-plugin para runtime y empaquetado nativo

See `BUILD_AND_PACKAGE.md` for packaging commands.
